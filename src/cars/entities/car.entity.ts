export class Car {
  id: string;
  price: number;
  modelName: string;

  constructor(id: string, price: number, modelName: string) {
    this.id = id;
    this.price = price;
    this.modelName = modelName;
  }
}
